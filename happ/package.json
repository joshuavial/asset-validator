{
  "name": "asset-validator-dev",
  "private": true,
  "workspaces": [
    "ui-dev",
    "ui-agent",
    "ui-user",
    "tests"
  ],
  "scripts": {
    "start": "AGENTS=2 BOOTSTRAP_PORT=$(port) SIGNAL_PORT=$(port) npm run network",
    "start:user": "UI_PORT=8080 npm start -w ui-user",
    "network": "hc s clean && npm run build:happ && UI_PORT=8888 concurrently \"npm start -w ui-dev\" \"npm run launch:happ\" \"holochain-playground\"",
    "test": "npm run build:zomes && hc app pack workdir --recursive && npm t -w tests",
    "launch:happ": "concurrently \"hc run-local-services --bootstrap-port $BOOTSTRAP_PORT --signal-port $SIGNAL_PORT\" \"echo pass | RUST_LOG=warn hc launch --piped -n $AGENTS workdir/asset-validator.happ --ui-port $UI_PORT network --bootstrap http://127.0.0.1:\"$BOOTSTRAP_PORT\" webrtc ws://127.0.0.1:\"$SIGNAL_PORT\"\"",
    "package": "npm run build:happ && npm run package -w ui-dev && hc web-app pack workdir --recursive",
    "build:happ": "npm run build:zomes && hc app pack workdir --recursive",
    "build:zomes": "RUSTFLAGS='' CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown"
  },
  "devDependencies": {
    "@holochain-playground/cli": "^0.1.1",
    "concurrently": "^6.2.1",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "npm": ">=7.0.0"
  },
  "dependencies": {
    "jsqr": "^1.4.0"
  }
}
